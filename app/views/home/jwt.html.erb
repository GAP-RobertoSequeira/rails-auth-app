<% if user_signed_in? %>

    You can use the following JWT to test:

    curl -X GET --header 'Authorization: Bearer <%= JWTWrapper.encode({ user_id: current_user.id }) %>' 'http://localhost:3000/api/users'
<% end %>
